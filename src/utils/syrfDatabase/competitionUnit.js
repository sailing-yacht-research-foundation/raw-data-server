/**
 *
 * @param {*} name name of race
 * @param {*} startTime start time
 * @param {*} endTime end time
 * @param {*} approximateStart
 * @param {*} approximateStart_utc
 * @param {*} approximateStart_zone
 * @param {*} timeLimit
 * @param {*} isCompleted
 * @param {*} boundingBox
 * @param {*} courseId
 * @param {*} calendarEventId
 * @param {*} vesselParticipantGroupId
 * @param {*} description
 * @param {*} approximateStartLocation
 * @param {*} approximateEndLocation
 * @param {*} country
 * @param {*} city
 * @param {*} openGraphImage
 * @returns
 */
function save(
  name,
  startTime,
  endTime,
  approximateStart,
  approximateStart_utc,
  approximateStart_zone,
  timeLimit,
  isCompleted,
  boundingBox,
  courseId,
  calendarEventId,
  vesselParticipantGroupId,
  description,
  approximateStartLocation,
  approximateEndLocation,
  country,
  city,
  openGraphImage,
) {}

module.exports = {
  save,
};
